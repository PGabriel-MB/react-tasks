import { RefreshCcw } from "react-feather";
import styled from "styled-components";

const Container = styled.div``

export const Loading = () => {
    return (
        <Container>
            <RefreshCcw id="refreshIcon" />
        </Container>
    )
}